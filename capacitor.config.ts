import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gamer_vivi.scannerapp',
  appName: 'Document Scanner',
  webDir: 'dist'
};

export default config;
